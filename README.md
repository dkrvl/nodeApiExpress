# nodeApiExpress

Add config/db.js
with below

module.exports = {
    url: `mongodb://<USERNAME>:<PASSWORD>@HOSTURL/<DBNAME>`
}

======================================================

**To Run project**

Just run command in terminal
`npm run dev`
